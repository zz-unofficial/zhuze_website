---
title: 'Project template'
description: 'Copy this directory to create a project detail page.'
draft: true
---

# Project title

Copy this directory to `src/content/projects/<slug>/`, rename the folder to match the `slug` in
`src/site.config.ts`, then replace this content with the project details.
